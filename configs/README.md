# Configs

This directory stores the auto-generated JSON configuration files which save your drawn line or polygon coordinates for each video. 
These are ignored by Git as they are generated on a per-video basis.
