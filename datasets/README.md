# Datasets

This directory will automatically store the output from the video processor, including cropped images of vehicles and generated Excel reports.
These files are ignored by Git to prevent the repository from becoming too large.
